program fibunacci()
global
	
	fun fib(copy a:int64) returns r:int64
 	do
    	if a <= 1 then
			r init := a
		else
			r init := fib(a - 1) + fib(a - 2)
		endif
 	endfun;
	
	var a:int64

do
	debugin a init;
	a := fib(a);
	debugout a
endprogram